query marketData($btcAmount: Float!) {
  bestMarket(btcAmount: $btcAmount) {
    btcAmount
    errors
    bidsBestMarketName
    asksBestMarketName
    bidsBestUSDAmount
    asksBestUSDAmount
    date
  }
}
